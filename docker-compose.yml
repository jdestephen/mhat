services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: mhat_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/mhat_db
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_SERVER=db
      - POSTGRES_DB=mhat_db
      - SECRET_KEY=${SECRET_KEY}
      - EMAIL_ENABLED=false
      - STORAGE_LOCAL_MODE=false
      - STORAGE_ENDPOINT=https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com
      - STORAGE_ACCESS_KEY=${R2_ACCESS_KEY}
      - STORAGE_SECRET_KEY=${R2_SECRET_KEY}
      - STORAGE_BUCKET=${R2_BUCKET}
    depends_on:
      - db

volumes:
  postgres_data:
